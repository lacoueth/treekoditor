import { ITTNode } from '../../models/ttAST.model';
/**
 * Serialize TipTap AST to raw text
 * @param node TipTap AST document node (root node)
 */
export declare function _ta2rt(node: ITTNode): string;
